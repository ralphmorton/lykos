{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "spago bundle-app --main Main --to build/index.js && parcel build static/index.html --no-cache --dist-dir ../static --public-url assets"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.3",
    "parcel": "^2.9.3",
    "purescript": "^0.15.10",
    "spago": "^0.21.0"
  },
  "dependencies": {
    "preact": "^10.17.1"
  }
}
